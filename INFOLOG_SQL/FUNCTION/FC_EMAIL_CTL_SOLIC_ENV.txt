CREATE OR REPLACE 
  FUNCTION FC_EMAIL_CTL_SOLIC_ENV 
RETURN NUMBER
AS
 I_CTL_SOLIC_ENV Number;
--
BEGIN
--
 SELECT SUPERVISOR.SQ_CTL_SOLIC_ENV.nextval
   INTO I_CTL_SOLIC_ENV
   FROM DUAL;
 RETURN(I_CTL_SOLIC_ENV);
--
END FC_EMAIL_CTL_SOLIC_ENV;
