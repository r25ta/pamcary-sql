CREATE OR REPLACE
FUNCTION FC_INICIO_VIAGEM_INCLUIR(PNUM_MCT NUMBER)
RETURN NUMBER
AS
CURSOR
ITERMINAL IS

SELECT num_mct, cod_macro_ult, to_char(dhr_macro_ult,'YYYYMMDDHH24MISS') AS dhr_macro_ult,cod_serve
FROM POSICAO_ID_TERMINAL
WHERE num_mct = PNUM_MCT

RTERMINAL ITERMINAL%ROWTYPE;

BEGIN
OPEN ITERMINAL;
FETCH ITERMINAL INTO RTERMINAL;

 WHILE ITERMINAL%FOUND LOOP
   
   IF RTERMINAL.dhr_macro_ult != ' ' THEN

   END IF;
 
 END LOOP;
 CLOSE ITERMINAL;
 
END;
/
